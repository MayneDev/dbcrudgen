	/*
	* Performs a database query and returns the value of QUERIED_COLUMN 
	* based on the value of PRIMARY_KEYS_PARAM_VALUES passed to the function
	*/
	public function get_QUERIED_COLUMN(PRIMARY_KEYS_PARAM_VALUES) {
		$columns = array (PRIMARY_KEYS_COLUMN_PARAMS);
		$records = array (PRIMARY_KEYS_PARAM_VALUES);
		$QUERY_RESULTS = $this->query_from_TABLE_NAME ( $columns, $records );
		return $QUERY_RESULTS [0] ['QUERIED_COLUMN'];
	}
	